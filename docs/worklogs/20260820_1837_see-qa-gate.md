---
date: 2026-08-20 18:37
type: worklog
status: complete
tags: [see, qa, gate, tests]
---

# Default gate now proves the Super Enhancement Engine

## Goal

Automate QA so `python3 tests/run_validation.py` is the no-false-fail suite:
SEE unit tests always run; real-title off/on boots run when media is present
and skip when it is not; cmake/SDL2 cases skip instead of failing.

## Done

- `tests/see_replacement.c` — added `reverted-locks` (shipped present path).
- `tests/run_validation.py` — default cases are `source cpu audio qa see boot`
  then `gpu sdl-audio chd zip`. Missing `sdl2-config` / `cmake` prints
  `skip reason=host-missing-…` and continues.
- `tests/boot_see_gate.py` + `make test-boot-see` — two off + two on of one
  local title (prefers SCUS-942.54). Skip exit 0 if no BIOS/disc.
- `tests/validate_sources.py` — asserts SEE/boot are in the default suite,
  host skips exist, and GP0(A0) still writes VRAM.

## Verified

Executed; output is real.

**`make test-see`** (`see-unit.log`):

```
SEE_REPLACEMENT passed case=off-passthrough
SEE_REPLACEMENT passed case=on-auto-upscale hash=4de67c53ba950399
SEE_REPLACEMENT passed case=disable-original
SEE_REPLACEMENT passed case=user-overrides-generated
SEE_REPLACEMENT passed case=reverted-locks
SEE_REPLACEMENT passed case=vram-unchanged
SEE_PACK passed serial=SCUS-942.54 hash=e7b7fa0cc9267a59 stripped=orig.png
```

**Four headless Legaia boots** (title frame 840, 320×228):

| run | hash |
| --- | --- |
| off-1, off-2 | `1c859cc02ebe806e` (golden) |
| on-1, on-2 | `2f51fa06d28bb700` (differ, agree) |

Both on-runs: `serial=SCUS-942.54 enhance=True applied=True`.

**Default suite** `python3 tests/run_validation.py` → exit 0, 130.240s:

```
passed source, cpu, audio, qa, see, boot
skip gpu,sdl-audio reason=host-missing-sdl2
skip chd,zip reason=host-missing-cmake
ARMSX_BOOT_SEE passed id=SCUS-942.54 off=1c859cc02ebe806e on=2f51fa06d28bb700 runs=2+2
```

## Next

Unchanged: Vulkan present of the same cache, once cmake/SDL2/Vulkan headers
exist on the host.
